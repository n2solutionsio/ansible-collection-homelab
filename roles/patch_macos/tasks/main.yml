---
# Placeholder — macOS patching is deferred
# Planned tasks:
# - softwareupdate --install --all (system updates)
# - brew update && brew upgrade (Homebrew packages)
# - brew cleanup (disk reclamation)

- name: macOS patching not yet implemented
  ansible.builtin.debug:
    msg: "patch_macos role is planned but not yet implemented"
